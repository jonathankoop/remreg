% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_selection.R
\name{evaluate_selection}
\alias{evaluate_selection}
\title{Evaluate Variable Selection Performance}
\usage{
evaluate_selection(selected)
}
\arguments{
\item{selected}{A list output from \code{select_variables()}, containing:
\itemize{
\item \code{mle}: Variables selected by the MLE method (if applicable).
\item \code{abr_horseshoe}: Variables selected by the ABR Horseshoe method (if applicable).
\item \code{abr_ridge}: Variables selected by the ABR Ridge method (if applicable).
\item \code{abr_lasso}: Variables selected by the ABR Lasso method (if applicable).
\item \code{parameters}: The original model parameters, where non-zero values indicate true effects.
\item \code{methods}: The methods used for selection.
}}
}
\value{
A list containing:
\itemize{
\item \code{tdr}: A list of True Discovery Rates (TDR) for each method.
\item \code{fdr}: A list of False Discovery Rates (FDR) for each method.
}
}
\description{
This function evaluates the performance of variable selection methods using
the output from \code{select_variables()}. It computes the True Discovery Rate (TDR)
and False Discovery Rate (FDR) for each selection method based on known non-zero
and zero parameters in the model.
}
\details{
The evaluation is performed by comparing the selected variables against the known
non-zero and zero parameters:
\itemize{
\item \strong{True Discovery Rate (TDR)}: The proportion of correctly identified non-zero parameters.
\item \strong{False Discovery Rate (FDR)}: The proportion of incorrectly identified zero parameters.
}

For each method in \code{selected$methods}, the function calculates:
\itemize{
\item \verb{TDR = (number of correctly identified non-zero variables) / (total number of non-zero variables)}
\item \verb{FDR = (number of incorrectly identified zero variables) / (total number of zero variables)}
}

The results are summarized and printed for user review.
}
\note{
This function assumes the \code{parameters} field in \code{selected} contains the true model
parameters, where non-zero values represent true effects and zero values represent
noise or irrelevant effects.
}
\examples{
# Example selected variables from select_variables
selected <- list(
  mle = c("send_z1", "difference_z2"),
  abr_horseshoe = c("send_z1"),
  abr_ridge = c("send_z1", "difference_z2", "inertia"),
  abr_lasso = c("send_z1"),
  parameters = list(
    send_z1 = 0.5,
    difference_z2 = 0.2,
    inertia = 0,
    reciprocity = 0
  ),
  methods = c("mle", "abr_horseshoe", "abr_ridge", "abr_lasso")
)

# Evaluate selection performance
performance <- evaluate_selection(selected)

# View results
print(performance$tdr)  # True Discovery Rates
print(performance$fdr)  # False Discovery Rates

}
\seealso{
\code{\link[=select_variables]{select_variables()}} for selecting variables based on significance criteria.
}
