% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_rems.R
\name{estimate_rems}
\alias{estimate_rems}
\title{Estimate Relational Event Models (REMs)}
\usage{
estimate_rems(
  edgelist,
  methods = c("mle", "abr_horseshoe", "abr_ridge", "abr_lasso"),
  seed = 123
)
}
\arguments{
\item{edgelist}{A list output from \code{generate_reh()}, containing:
\itemize{
\item \code{edgelist}: A data frame of simulated events with columns \code{time}, \code{actor1}, and \code{actor2}.
\item \code{parameters}: A named list of model parameters.
\item \code{covar}: A data frame of actor covariates.
\item \code{directed}: Logical indicating whether the network is directed.
}}

\item{methods}{A character vector specifying the estimation methods. Options include:
\itemize{
\item \code{"mle"}: Maximum Likelihood Estimation.
\item \code{"abr_horseshoe"}: ABR with Horseshoe prior.
\item \code{"abr_ridge"}: ABR with Ridge prior.
\item \code{"abr_lasso"}: ABR with Lasso prior.
Defaults to all methods.
}}

\item{seed}{An integer for reproducibility of random processes. Defaults to 123.}
}
\value{
A list with the following elements:
\itemize{
\item \code{coefs_mle}: Coefficients from the MLE model (if computed).
\item \code{coefs_abr_horseshoe}: ABR summary with Horseshoe prior (if computed).
\item \code{coefs_abr_ridge}: ABR summary with Ridge prior (if computed).
\item \code{coefs_abr_lasso}: ABR summary with Lasso prior (if computed).
\item \code{abr}: Raw ABR results for each prior type used.
\item \code{parameters}: Model parameters used for estimation.
\item \code{covar}: Actor covariates from the input REH.
\item \code{methods}: Methods used for estimation.
\item \code{directed}: Indicates whether the network is directed.
\item \code{seed}: Random seed used during the process.
}
}
\description{
This function estimates Relational Event Models (REMs) using Maximum Likelihood Estimation (MLE) and Approximate Bayesian Regularization (ABR) with various priors. It processes a relational event history (REH) generated by \code{generate_reh()} and computes tie-oriented statistics to fit the specified model.
}
\details{
The \code{estimate_rems()} function performs the following steps:
\enumerate{
\item \strong{Formula Generation}: It generates a formula for tie effects using \code{generate_formula()} based on the input parameters.
\item \strong{Statistics Computation}: Computes tie-oriented statistics using \code{remstats::remstats()} on the input REH.
\item \strong{MLE Estimation}: If \code{"mle"} is included in \code{methods}, the function estimates the model using \code{remstimate::remstimate()} and extracts the coefficients and covariance matrix for further processing.
\item \strong{ABR Estimation}: If \code{"abr_horseshoe"}, \code{"abr_ridge"}, or \code{"abr_lasso"} are included in \code{methods}, the function performs Approximate Bayesian Regularization using \code{shrinkem::shrinkem()} with the specified priors.
}

\strong{MLE Method}:
Estimates model parameters using maximum likelihood and calculates their covariance matrix. These estimates serve as priors for ABR methods.

\strong{ABR Method}:
Incorporates prior distributions to shrink model parameters. Three priors are supported:
\itemize{
\item \strong{Horseshoe}: Enforces sparsity in model coefficients.
\item \strong{Ridge}: Applies global shrinkage to all coefficients.
\item \strong{Lasso}: Promotes sparsity for interpretable results.
}

Methods not included in \code{methods} are skipped.
}
\note{
This function assumes that the input REH is correctly formatted and generated by \code{generate_reh()}. Providing invalid inputs may result in errors.
}
\examples{
# Simulate a REH using generate_reh
parameters <- list(
  baseline = -5,
  send_z1 = 0.5,
  difference_z2 = 0.2,
  inertia = 0.3
)
covar <- data.frame(
  name = 1:10, time = 0,
  z1 = rnorm(10),
  z2 = rnorm(10)
)
reh <- generate_reh(parameters, covar, M = 20, directed = TRUE)

# Estimate REMs
results <- estimate_rems(reh, methods = c("mle", "abr_horseshoe"))

# View results
print(results$coefs_mle)                # MLE coefficients
print(results$coefs_abr_horseshoe)      # ABR Horseshoe summary

}
\seealso{
\code{\link[=generate_reh]{generate_reh()}} for generating relational event histories.
\code{\link[remstats:remstats]{remstats::remstats()}} for computing tie-oriented statistics.
\code{\link[remstimate:remstimate]{remstimate::remstimate()}} for estimating REMs using MLE.
\code{\link[shrinkem:shrinkem]{shrinkem::shrinkem()}} for Approximate Bayesian Regularization.
}
