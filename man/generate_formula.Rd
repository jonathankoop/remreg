% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_formula.R
\name{generate_formula}
\alias{generate_formula}
\title{Generate a Formula for \code{tie_effects} in \code{remstats}}
\usage{
generate_formula(parameters)
}
\arguments{
\item{parameters}{A named list of parameters and their assigned values.
Parameter names must follow a specific naming convention to be recognized
as valid \code{remstats} effects. For example, endogenous names could \code{inertia} or
\code{psABBA}. Exogenous statistics need to have the respective statistic name from
\code{remstats::tie_effects(endogenous = FALSE)} followed by \verb{_[variable]}, e.g.
\code{send_z1}. Unrecognized names will be ignored.}
}
\value{
A formula object of class \code{"formula"}, suitable for the \code{tie_effects}
argument in the \code{remstats::remstats} function. The formula specifies the
effects in the tie-oriented model for which statistics are computed.
}
\description{
This function creates a formula for the \code{tie_effects} argument in the
\code{remstats} package based on a list of parameters and their corresponding values.
It translates parameter names into valid \code{remstats} effect terms
and combines them into a formula.
}
\details{
The \code{generate_formula()} function translates parameter names into effect terms
using specific prefixes or direct matches. The following naming conventions
are supported:
\itemize{
\item \strong{Prefixes}: Certain prefixes indicate exogenous effects:
\itemize{
\item \code{difference_}: Generates a \code{difference('\%s', scaling = 'std')} term.
\item \code{same_}: Generates a \code{same('\%s')} term.
\item \code{send_}: Generates a \code{send('\%s', scaling = 'std')} term.
\item \code{receive_}: Generates a \code{receive('\%s', scaling = 'std')} term.
\item Additional prefixes like \code{tie_}, \code{average_}, \code{minimum_}, \code{maximum_},
and \code{event_} are also supported.
}
\item \strong{Specific Terms}: Endogenous effects are recognized directly by their names, such as:
\itemize{
\item \code{"inertia"}, \code{"indegreeSender"}, \code{"reciprocity"}, \code{"sp"}, etc.
\item These names map to their respective terms in \code{remstats}.
}
\item \strong{Standardization}: All effects that support the \code{scaling} argument
are automatically standardized by default (i.e., \code{scaling = 'std'} is applied).
This ensures that the effects are computed consistently within the model.
}

Any parameter names that do not match a prefix or specific term will be ignored.

The formula always includes an intercept (\code{1}) by default.
}
\examples{
# Define parameters
parameters <- list(
  baseline = -5,
  send_z1 = 0.5,
  difference_z2 = 0.2,
  inertia = 0.3,
  receive_z1 = 0,
  unknown_param = 1  # This will be ignored
)

# Generate formula
formula <- generate_formula(parameters)
print(formula)
# Output:
# ~ 1 + send('z1', scaling = 'std') +
#   difference('z2', scaling = 'std') +
#   inertia(scaling = 'std') +
#   receive('z1', scaling = 'std')

# Use the formula in remstats
# remstats::remstats(reh, tie_effects = formula, attr_actors = covar)

}
\seealso{
\code{\link[remstats:remstats]{remstats::remstats()}} for computing statistics based on the generated formula.
}
